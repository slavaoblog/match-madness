spring:
  profiles:
    default: 'development'
  datasource:
    url: 'jdbc:h2:mem:test'
    username: ''
    password: ''
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      format_sql: true
  output:
    ansi:
      enabled: always
logging:
  level:
    org:
      hibernate:
        SQL: debug